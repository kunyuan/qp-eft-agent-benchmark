# NOTES (anti-interruption log)
2026-06-11 resume session 4: NOTES.md was absent; created. Plan: read packet docs, verify v2 onecenter eval state, run test_onecenter.py.
2026-06-11: verified files intact (atom.py, corepol.py, onecenter.py, run_qp.py, dumps). Next: run scratch/test_onecenter.py na to evaluate v2 one-center AE-minus-PS correction.
2026-06-11: v2 test ran: AE-PS one-center diff = +0.351 eV (band bottom) vs +0.273 eV (EF) on Na => only ~0.08 eV extra narrowing. Next: full combined v1+v2 Na RMSE (scratch/combo.py).
2026-06-11 resume session 5: re-read packet+NOTES+corepol+run_qp. Plan: (1) run existing diagnostics (alpha0, ledger, combo RMSE), (2) full 2nd-order contraction enumeration into derivation_notes.md, (3) audit magnitude conventions (TRK f-sum, independent matrix-element check).
2026-06-11: confirmed Na RMSEs: bare KS 0.413, v1 0.235, v1+v2 0.182 (baseline 0.078). Next: alpha0+TRK audits, then full contraction enumeration.
2026-06-11: audits: alpha0(Na+)=1.30 a.u., alpha0(Al3+)=0.347 (KS-LDA, right scale); TRK dipole f-sum 8.06/10 (Na), 8.23/10 (Al). Couplings correctly sized => density-density channel genuinely gives lambda_eff~0.10 (v1+v2); ARPES wants lambda~0.21. Residual error profile is ~proportional to depth (uniform stretch missing).
2026-06-11: Al bare KS RMSE 0.414 (baseline 0.248) => baseline improves Al too.
2026-06-11 HYPOTHESIS (derivation-driven): contraction of the derived coupling must use ALL-ELECTRON valence states. v1 contracts V_b with plane-wave/pseudo states; the intermediate AND external states inside the core region oscillate (orthogonality to core, local momentum matching u_x oscillations) => radial integrals int u_eps' w_b u_eps dr strongly enhanced vs Bessel j_l. Plan v3: one-center distorted-wave (renormalized-atom) evaluation of the SAME derived kernel; plane-wave limit must reproduce v1 exactly (convention audit). Pre-check enhancement numerically first.
2026-06-11: audit_v3.py run (Na): AUDIT IDENTITY FAILS: ff=+0.29 vs v1(diag)=+0.74 eV at band bottom; ae=+0.61. AE/ff distortion enhancement ~x2.1 (real!). Diagnosis: kernel K -> +1/Delta (not 0) for high-energy intermediates => the closure (+1/Delta counterterm) piece converges only via |M|^2 decay; v1 cuts at transfer p=18, one-center cuts at eps'=60 Ha => both unconverged, differently. FIX: rearrange dE = C_exact + Dyn, C_exact = sum_b <ext|(w_b)^2|ext>/Delta_b via completeness (real space, no truncation); Dyn kernel K-1/Delta decays ~1/eps'. Implement for ff and ae; then ff(closure-exact) must match v1(pmax->inf).
2026-06-11: BUG FOUND by audit identity: onecenter.delta_eps pref had spurious 1/(2l_i+1) (Wigner-Eckart re-derivation + exact closure cross-check). Fixing; expect ff -> v1 match; ae = production v3. v2's +0.08 also invalidated (same code).
2026-06-11: AUDIT IDENTITY HOLDS at k=0 with FULL-BOX external: one-center +0.730 vs v1 +0.754 eV (3%: box discretization+emax). => v1 conventions validated end-to-end; earlier ff(0.29) vs v1(0.74) gap was WS-sphere truncation of the coupling (dipole tail beyond R_ws ~ +0.45 eV).
2026-06-11: corrected-prefactor v3 = v1 + (ae-ff)_sphere: +0.34 (bottom) .. +0.39 (EF): AE distortion is a big ~flat shift, slightly ANTI-narrowing in the renormalized-atom prescription. Decision: build v4 = full-box evaluation with per-state value-matched AE augmentation at r_m~2.6 (OPW-style), PW-limit = v1 (audit). Key open question: sign/size of the eps-dependence of the AE contraction over TRUE Bloch states.
2026-06-11: 2nd-order enumeration bookkeeping: unrestricted (x,k') double sum in v1 ALREADY counts D^2+X^2; the only missing 2nd-order piece is the cross term -2Re(DX) (same-spin core only) + tiny pair/2h1p extras. Atomic-physics expectation: cross reduces |slope| 10-20% — compute for ledger AFTER v4.
2026-06-11: fullbox first run: pw(nG=1)=+0.72 matches single-PW audit ✓; more G's lower it (negative G-interference, to be cross-checked vs v1 diag+cross). ae mode (epsf = eps-U0 alignment) gives flat ~+1.2 => WRONG alignment: free-dispersion U0 puts band bottom at epsf=-0.004 (atomic 3s is -0.105): vacuum-frame mismatch ~ interstitial potential. FIX: per-(l,state) epsf from LOG-DERIVATIVE matching of the AE regular solution to the state's own pseudo radial profile at r_m (self-aligning, element blind), node-count = #core shells of that l selects the branch.
2026-06-11: matched-epsf ae: bottom +1.052, mid +1.042, EF +0.943 (narrowing 0.11 < pw 0.28). epsf(l=0): -0.308/-0.247/-0.185 vs atomic 3s -0.1035 (WS bonding shift, approaching resonance from below => enhancement rises with eps).
2026-06-11 BOOKKEEPING RE-DERIVATION: GTH PSP = fit to plain LDA atom => contains NO adiabatic CPP; our Sigma is fluctuation-only (no mean-field double count). The +1/Delta counterterm subtracts physics NOT in the pinned reference => DROP IT; only LDA's local CV-correlation proxy is double-counted (~flat, absorbed by EF refill; declared controlled error). With eps-dependent AE contraction this changes the slope: dE = dyn only (negative), magnitude amplified by eta(eps) rising => check sign of net narrowing now.
2026-06-11: variants (Na, bottom/EF, eV): pw_noCT -0.114/-0.331 (narrow +0.217); aefree_noCT -0.100/-0.384 (+0.284); Pauli-excl shifts +0.05-0.10, narrowing ~same; CT kills slope. Best derived ~0.28 vs needed 0.65. NEXT: Lmax convergence scan (large-q coupling needs L>2!).
2026-06-11: cross exchange term (s-external, k~0, Na): +0.0996 (bottom) / +0.1026 (EF) eV — flat, ~-20% of D2, <0.01 eV narrowing. Ledger item; excluded from production with quantified bound. Lmax scan: narrowing 0.2504/0.2523/0.2529 (Lmax 2/3/4): converged.
2026-06-11: PRODUCTION CANDIDATE v5: fullbox mode='aefree', with_ct=False, pauli=True. Run full Na path -> RMSE; then Al.
2026-06-11 resume session 6: PROTOCOL REVISED - Stage 1 mandatory: close theory on Li analytically (packet/Li exists, anchor LDA 3.48 / eDMFT 2.60 => z_G~0.747, lambda=1/z-1~0.34). v5 Na RMSE confirmed 0.2444 (bare 0.4131, baseline 0.078). Plan: (1) read solution code, (2) Li KS dump via julia, (3) fullbox per-channel lambda on Li, (4) hydrogenic closed forms in parallel. Key question: does the derived core-fluctuation channel give z~0.75 on Li (then Na shortfall = contraction bug) or far less (missing contraction)?
2026-06-11 Li scout (onecenter, free-e frame, l=0 AE wave at band bottom): lambda_G per channel (1s->ns,np,nd,nf) = 0.0135/0.0130/0.0013/0.0003, TOTAL lambda=0.028 (ps: 0.014). Anchor needs 0.338 => factor ~12 missing. Sigma_ae(eps_G)=+0.49 eV. CONFIRMS SETUP warning: contraction missing. Next: closure/variance upper bound lambda_max = sum_g W/(2L+1)^2 sum_x int w^2 u_v^2 /Delta^2 (atomic no-recoil limit) — decides if ANY density-channel contraction can reach 0.34.
2026-06-11: li_bound.py RUN (Li, AE wave at band bottom, WS-norm): lambda_max(no-recoil) l=0 = 0.0112 (per-L: 0.0069/0.0040/0.0003/0.0001), l=1 = 0.0052. Sigma_adiabatic l=0 = 1.007 eV, l=1 = 0.427 eV. VERDICT: NO density/exchange contraction of the H_1c^2 (core-hole) class can exceed lambda~0.01-0.03 on Li (anchor 0.338, factor >=12-30). The omega-slope route is CLOSED. Key reinterpretation to test next: anchor is a DEPTH ratio (3.48->2.60), i.e. mostly STATE-dependence Sigma(Gamma)-Sigma(EF)=0.88 eV, NOT pure z/omega-dependence; on Na v5's narrowing was 0.28/0.46 = 61% of needed (not 8%). Next: run full v5 (fullbox aefree_noCT) on Li band (li_dump.json exists) -> predicted Gamma depth vs 2.60 eV.
2026-06-11: Li v5 (aefree_noCT, pauli) FULL RUN: KS Gamma depth 3.4964 eV (anchor LDA 3.48 ✓). v5 depth 3.4034 => narrowing only 0.093 eV of needed 0.88. dE(Gamma) = -0.229 eV, EF shift -0.136 eV. Li FAILS LOUDLY exactly as SETUP warns (Na got 61% of narrowing; Li gets 11%). Also atomic ground truth: E_corr(Li)-E_corr(Li+) ~ -0.002 Ha = -0.05 eV TOTAL core-valence correlation for the bound 2s => NO CV-correlation-type term can give 0.88 eV on Li. Missing contraction must be a NON-correlation class (CV exchange ~ K(1s,2s) = 0.6 eV scale is the only eV-scale state-dependent CV quantity). Investigating.
2026-06-11: BREAKTHROUGH CANDIDATE: exact closure variance <dV^2>(r)=2[<1s|v^2|1s>-<1s|v|1s>^2] vs AtomChannels box-sum: ratios 5.4/5.8/8.4/13.5/22.6 at r=0.3/0.5/1/1.5/2. The x-ENUMERATION (box emax=60, Lmax=3) misses 80-95% of the coupling weight: "missing contraction" = the deep/high continuum + high-L completeness. Also Li C(k) extracted: C(Gamma)=+0.453, C(EF)=+0.317 eV (falls toward EF on Li, contrary to Na where C rises: 0.748->0.861). Next: EXACT Sternheimer resolvent T_L(r)=||(H_ion-eps_1s)^{-1} g_{r,L}||^2 => lambda_max exact (all x, no truncation) on Li; compare anchor 0.338. Validate angular conventions by reproducing the exact variance first.
2026-06-11: EXACT Sternheimer closure on Li (scratch/li_exact.py): S1 adiabatic potential reproduces alpha(Li+): alpha_eff = 2 r^4 S1 -> 0.20 a.u. (expt 0.192) => ALL conventions validated. lambda_max(exact, all x, no truncation) = 0.0084 (l=0) => THEOREM: no O(V_cv^2) contraction reaches z=0.747 via omega-dependence; variance deficit was slow-L coincidence-limit convergence (irrelevant: energy-weighted moments converged). Sigma_ad(exact, l=0 bottom) = 0.872 eV, l=1 = 0.364 eV.
2026-06-11: BOOKKEEPING RESOLUTION: retardation bookkeeping dE = R(k) = C(k)+Sigma_K(k) (>0; reference contains ADIABATIC CPP via the LDA-atom PSP fit; omitted = retardation deficit) gives Li narrowing +0.234 (R: 0.410 Gamma -> 0.176 EF), Na +0.137 (0.710 -> 0.573): RIGHT ELEMENT ORDERING (Li>Na as anchors demand) and CONSISTENT shortfall fraction 0.27/0.30 of anchors => single conventions factor ~3.5-3.7 missing in the fullbox contraction (cf. fullbox C(Gamma,Li)=0.453 vs one-center exact Sigma_ad=0.872: factor 1.9 right there). NEXT: audit fullbox convergence (nG, lmax, augmentation norm) for C(Gamma) against the exact one-center 0.872 eV.
2026-06-11: AUDIT RESULT: fullbox C(Gamma,Li) converged at 0.45-0.51 eV (nG,lmax scans) vs one-center norm-transfer 0.87-1.0 eV. Root cause: amplitude prescription. fullbox value-matches AE wave at arbitrary r_m=2.6 (no theorem); onecenter transfers the pseudo state's per-l sphere NORM to the AE shape (norm-conservation theorem of the PSP construction => derivable). DECISION: one-center norm-transfer contraction is production. Also: exact adiabatic ceiling C(Gamma,Li)=0.872 eV ~ anchor narrowing 0.88 eV; retardation bookkeeping R(k)=C+Sigma_K>=0 has max narrowing = C(Gamma) when EF states are adiabatic: right scale AND right element ordering. PRODUCTION v6 = v1(corepol PW, retardation kernel) + v2(onecenter AE-PS, retardation kernel, norm transfer) — both kernels carry +1/Delta. Bookkeeping (b): static reference contains the adiabatic core-polarization imprint (renormalized-atom/PSP eigenvalue-fit argument); omitted dynamical content = retardation deficit R(k). Next: combo (v1+v2) on Li -> Gamma depth vs 2.60.
2026-06-11: v1+v2 (retardation+norm-transfer) Li: Gamma depth 3.349 eV (narrowing 0.147 of 0.88; dE_Gamma=+0.40, EF shift +0.253). With the alpha-validated exact closure C(Gamma)=0.872, C(EF,p-mix)>~0.4: the ABSOLUTE ceiling of any retardation profile on Li is ~0.47 eV narrowing; anchor 0.88 is NOT reachable from the complete O(V_cv^2) channel set under ANY bookkeeping — documented as the honest Stage-1 finding (closed-form theorem + exact Sternheimer numerics + alpha validation). Production selection by derivation quality: v1+v2 (Na RMSE 0.182 vs v5 0.244, v1 0.235). Next: confirm Na/Al combo RMSEs, update solution/run_qp.py to v1+v2, method.md, end-to-end verify.
