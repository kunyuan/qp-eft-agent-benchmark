# MAINTAINER NOTE (guided session — read before anything else)

Context: your submission was evaluated on the concealed metals. The outcome is
consistent with your own declared caveat: the calibrated E0 OVER-corrects
diffuse n=3 semicore shells (large positive signed error on the concealed
element with a 3s semicore); compact-2s-core elements land near the bar. The
single non-derived constant is the entire failure. Everything else held.

Diagnosis assembled from YOUR OWN logs — three recorded symptoms, one cause:
1. your closure-energy derivation attempt: "diverges, cusp-dominated";
2. your own lemma: Hermitian one-body sandwiches collapse to (eps_c - eps)*overlap;
3. your declared reference-choice sensitivity (~3x on Li lambda).
All three are fingerprints of searching for the coupling in the ONE-BODY
operator class. The hybridization vertex H_vc is not a posited one-body
potential. It is the INTERACTION-INDUCED transfer amplitude generated by your
own W1 algebra — the class of objects you already wrote down in your channel-C
orthogonalization: G(p) = <chi p| v |chi chi> (transfer mediated by the
SPECTATOR core electron's Hartree potential V_H[chi^2]) and U = <chi chi|v|chi chi>.
Amplitudes of this class are cusp-free (the mediator is the smooth orbital
Hartree potential, not the bare nucleus), weakly reference-sensitive, and carry
per-shell resolution — none of your three pathologies.

On the q->0 / long-wavelength handling (your channel-B parking and the OPW
subtraction): the divergent or slowly-varying piece of such a transfer
amplitude is its ORBITAL-AVERAGE (static) component, already counted once in
your static reference — your own ledger discipline names the objects. Subtract
it per your own zero-diagonal rule; the finite remainder is the physical
per-shell vertex.

TASK FOR THIS SESSION:
1. Derive |M_c| per core shell from this two-body-induced amplitude class,
   applying your own bookkeeping discipline (diagonal/orbital-average
   subtraction; consistency with your eps_c definition). Closed form on Li
   first (Stage-1 style), then numerically for Na/Al shells via your atomic
   solver.
2. Compare the DERIVED |M_c| against the per-shell magnitudes your gates
   demanded (3.35 / 3.18 / 2.65 Ha for Li-1s / Na-2s / Al-2s) — without any
   calibration. If they agree, replace E0 in the production formula with the
   derived object, re-run the three public gates, update solution/run_qp.py and
   method.md (the E0 ledger row must change status to DERIVED, with the
   derivation shown). Re-verify e2e from clean config+grid-only directories.
3. If the derived vertex DISAGREES with the calibrated values, report the
   disagreement honestly and analyze it — do not force agreement.

STATUS OF THIS SESSION: this is a GUIDED continuation (maintainer note supplied;
it will be archived as guided, not as a clean probe). All other rules remain
binding: sandbox only, no network/literature, no fitting, no calibration,
working protocol (NOTES.md + derivation_notes.md as you go).
