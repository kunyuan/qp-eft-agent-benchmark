version = "1.0"

[metadata]
author_name = "QP-EFT Benchmark"
author_email = "noreply@example.com"
difficulty_explanation = "Reconstruct and implement the frozen-core quasiparticle correction. The closed-form correction is given but the core form factor is NOT; compute it from the provided atomic core data, assemble the renormalization on DFTK Kohn-Sham bands, and beat bare KS against held-out ARPES on concealed metals."
category = "scientific-computing"
tags = ["physics", "dft", "dftk", "quasiparticle", "frozen-core", "julia", "level-2"]

# DFTK SCF + bands on the two hidden metals (K, Mg) take a few minutes each.
# no-network: the submitted run_qp.py runs OFFLINE here, so it cannot fetch the
# held-out answers online (e.g. git clone the authors' eft-psp repo). DFTK works
# offline — the pinned env + GTH pseudo artifact are baked into the image at build.
[verifier]
timeout_sec = 2400.0
network_mode = "no-network"

# The agent keeps network (it needs to reach its own model API). During the agent
# phase only Na/Al are visible, so the concealed K/Mg answers can't be fetched;
# and any network call its code makes will fail at verify time (no-network above).
[agent]
timeout_sec = 7200.0

# Building the image instantiates + precompiles DFTK and pre-fetches artifacts.
[environment]
build_timeout_sec = 3600.0
